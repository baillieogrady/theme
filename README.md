# theme

Custom WordPress theme and vite.js setup with live reload.

## installation
npm i

## development
npx vite --host

add ```dev=1``` to the url to load in dev assets

## build
npx vite build

remove ```dev=1``` from the url to load in build assets
